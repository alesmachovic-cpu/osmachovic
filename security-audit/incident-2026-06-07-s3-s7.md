# Záznam o bezpečnostnom incidente — anonymné endpointy bez auth (2026-06-07)

> **Rozsah (aktualizovaný 2026-06-07):** Jeden konsolidovaný incident — viacero endpointov bez
> autentifikácie, objavených v ten istý deň, s rovnakou koreňovou príčinou (handler cez service-role
> bez `requireUser`/scope, resp. obídený CRON_SECRET). Nálezy: **S3** (klient-udalosti GET),
> **S7** (volni-klienti cron), **S4** (volni-klienti POST), **matching** (summary, objednavka/[id],
> nehnutelnost/[id]). Prvý hotfix (17:15:54 UTC) pokryl LEN S3/S7 — bol NEÚPLNÝ; S4+matching ostali
> živé do rozšíreného hotfixu. Bezpecnost robí kompletný sken všetkých anonymných endpointov.

> **Účel:** Záznam podľa GDPR čl. 33 ods. 5 (dokumentácia incidentu vrátane rozhodnutia o
> (ne)oznámení a jeho odôvodnenia) + interná evidencia, kým nie je na prode nasadená tabuľka
> `breach_register` (migr. 108 je zatiaľ len na dev). Po nasadení preniesť do tabuľky.
>
> **Pozn.:** Toto NIE je právny posudok advokáta — je to compliance záznam a odporúčanie. Finálne
> rozhodnutie o (ne)oznámení ÚOOÚ má ratifikovať právnik.

## 1. Základné údaje
- **Dátum zistenia:** 2026-06-07 (interný audit, okná Bezpecnost / Klienti / Compliance).
- **Zaznamenal:** Compliance (toto okno) na podnet MD; technicky overila Bezpecnost.
- **Prevádzkovateľ (controller):** Vianema s.r.o.
- **Spracovateľ (processor):** Machovic s.r.o. (prevádzkovateľ CRM).
- **Dotknutý systém:** os-machovic CRM, ostrá doména `vianema.amgd.sk` (deploy z branchu `main`).

## 2. Popis incidentu
- **S3 — `/api/klient-udalosti` GET bez autentifikácie.** Handler číta cez service-role (obchádza
  RLS), bez `requireUser` a bez `company_id` filtra. Anonymný požiadavka s platným `klient_id`
  (UUID) vráti históriu interakcií klienta vrátane voľnotextových poznámok, cross-tenant.
  Overené živým `curl` = HTTP 200 bez prihlásenia (kontrolný `/api/klienti` = 401).
- **S7 — `/api/cron/volni-klienti` verejne spustiteľný.** Bypass `?key=__internal__` obchádza
  `CRON_SECRET`. Anonymný spúšťač mení stavy klientov (cross-tenant) + spúšťa push/email.
  Overené živým `curl` = HTTP 200 bez secretu.
- **S4 — `/api/volni-klienti` POST bez auth (na main/prode).** Anonymne prepriradí/uvoľní klienta
  naprieč firmami (zmena stavu/makléra). Na `dev` už opravené (requireUser + company_id scope).
- **matching — `/api/matching/summary`, `/api/matching/objednavka/[id]`,
  `/api/matching/nehnutelnost/[id]` bez auth (na main/prode).** Anonym stiahne mená + telefóny
  predávajúcich aj kupujúcich. Na `dev` už opravené (requireUser + company_id scope; summary je
  id-gated cez `?objednavky=ids`). Starý prod-variant (bulk vs id-gated) — over Bezpecnost (kľúčové
  pre riziko, viď sekcia 5).
- **🔴 SYSTÉMOVÝ ROZSAH (kompletný sken Bezpecnost, 2026-06-07):** NIE pár dier — prod je PLOŠNE
  nezabezpečený. **~12 live-anonymných klientskych/PII endpointov** na vianeme: okrem
  S3/S4/S7/matching aj **objednavky, odberatelia (IČO/DIČ/email), manazer/sla, calendar/obhliadky,
  makler-provizie-pct, logy (telefóny klientov), pobocky, firma-info**. **Koreňová príčina:** celá
  security-hardening vrstva existuje len na `dev` a nikdy nebola nasadená na `main`/prod (`main` ~93
  commitov za `dev`). Viaceré sú ZOZNAMOVÉ → pravdepodobne bulk-enumerable (anonym stiahne celé
  datasety bez UUID). Plný zoznam + okná expozície doplní Bezpecnost po komplet hotfixe.

## 3. Dotknuté údaje a osoby
- **Kategórie:** identifikačné/kontaktné údaje klientov, história interakcií, voľnotextové poznámky.
  **NIE** osobitná kategória (čl. 9) — žiadne rodné čísla/zdravie cez tieto endpointy.
- **Dotknuté osoby:** klienti RK na vianeme. Prod DB obsahuje **zmiešané reálne + test dáta**
  (62 klientov, ~28 reálne vyzerajúcich — platný SK mobil, reálne emaily). ⇒ reálne dotknuté osoby
  sú v hre; nedá sa odpísať ako čisto testovacie.

## 4. Okno expozície (potvrdené Bezpecnosťou, git history)
- **S3** (`klient-udalosti` GET bez auth): živé na `main` od **2026-05-11** (commit `60e1b49`) po
  nasadenie hotfixu **2026-06-07**. Dĺžka ≈ **27 dní**.
- **S7** (cron `?key=__internal__` bypass): živé na `main` od **2026-04-26** (commit `e2667b4`) po
  nasadenie hotfixu **2026-06-07**. Dĺžka ≈ **42 dní**.
- **Koniec okna:** **2026-06-07 17:15:54 UTC** — hotfix nasadený na ostro (commit `b5c7be9` na
  `main`, git auto-deploy, per-env vars OK), curl-overené: S3 GET bez cookie → **401**, S7
  `?key=__internal__` → **401** (bypass mŕtvy).
- **KOMPLETNÉ OKNÁ EXPOZÍCIE (Bezpecnost, finálne 2026-06-07).** Všetky zatvorené 4 hotfix commitmi,
  curl-overené 401, v okne **17:15–18:23 UTC dňa 2026-06-07**. Začiatok = first-commit na `main`:

  | Endpoint | Od (na main) | Charakter |
  |---|---|---|
  | `kolize/check` + `schvalit` | 2026-04-04 | id-gated |
  | `odberatelia` | 2026-04-09 | id-gated |
  | `inzerat/save` | 2026-04-24 | zmena dát |
  | `obhliadky/pdf` | 2026-04-25 | id-gated (PDF) |
  | S7 cron, `manazer/sla`, S4 (`volni-klienti` POST) | 2026-04-26 | sla bez dát / zmena |
  | `objednavky`, `sign/status` | 2026-05-08 | **objednavky = BULK (pseudonymné, 3 zázn.)** |
  | matching ×3 (summary, objednavka/[id], nehnutelnost/[id]) | 2026-05-10 | id-gated |
  | S3 `klient-udalosti`, `makler-provizie-pct`, `pobocky` | 2026-05-11 | id-gated / count |
  | `firma-info` | 2026-05-14 | údaje firmy |

- Najdlhšie okno ≈ 64 dní (kolize, od 4.4.). Jediný BULK = `objednavky` (pseudonymné). Ostatné
  id-gated, `logy` = len počty.
- Všetky boli anonymne exploitovateľné na ostrej vianeme (service-role obchádza RLS) počas svojho okna.
- ✅ **Podmienka „všetky anonymné P0 zavreté pred reálnymi klientmi" = SPLNENÁ** (0 anonymných
  klientskych-PII routes na prode, overené Bezpecnosťou). Deferred (market/AI/monitor) nequery-ujú
  klientske PII → idú s veľkým releasom.

## 5. Posúdenie rizika
- **Charakter:** bezpečnostná ZRANITEĽNOSŤ nájdená interne, **bez dôkazu skutočného neoprávneného
  prístupu**. Žiadny indikátor zneužitia.
- **Zmierňujúce:** S3 je UUID-gated (klient_id sa nedá enumerovať); malý interný okruh (makléri);
  dáta nie sú osobitná kategória; RLS je čistá (expozícia len cez S3/S7, nie plošný anon SELECT).
- **Priťažujúce:** živé na verejnej doméne; reálne PII; **dlhé okno expozície** (S3 ~27 dní,
  S7 ~42 dní) — nie krátkodobý prešľap; **chýba audit log čítaní** → skutočný prístup sa nedá
  pozitívne vylúčiť.
- **Vplyv dĺžky okna:** dlhšie okno zvyšuje pravdepodobnosť prístupu a trvanie rizika. ALE pre S3
  ostáva kľúčový mitigant — UUID-gating: ani za 27 dní sa endpoint nedá masovo zneužiť bez znalosti
  konkrétnych `klient_id` (nie sú enumerovateľné). S7 nečíta PII (mení stavy/posiela notifikácie) →
  jeho expozícia je skôr integrita/abuse než únik dôvernosti.
- **Rozšírené nálezy (S4 + matching):** S4 (volni-klienti POST) = anonymná ZMENA dát cross-tenant
  (integrita, nie únik). matching endpointy = únik mien+telefónov predávajúcich aj kupujúcich.
  **KĽÚČOVÁ OTVORENÁ OTÁZKA pre riziko:** bol starý prod-variant `matching/summary` **id-gated**
  (vyžadoval znalosť konkrétnych UUID → riziko ako S3, nízke–stredne nízke) alebo **bulk-enumerable**
  (vrátil zoznam všetkých klientov bez ID → VYSOKÉ riziko, hromadný anonymný dump kontaktov)?
  Na `dev` je id-gated, ALE starý prod kód treba overiť (Bezpecnost). **Ak bol bulk → posúva
  matching k „zvážiť oznámenie ÚOOÚ" a celé posúdenie sa sprísňuje.** Do potvrdenia toto pole drží
  posúdenie ako predbežné.
- **🔴 SYSTÉMOVÝ ROZSAH (aktualizácia 2026-06-07):** kompletný sken odhalil **~12 anonymných PII
  endpointov**, viaceré ZOZNAMOVÉ → pravdepodobne bulk-enumerable. Tým **PADÁ hlavný mitigant
  (UUID-gating)**, o ktorý sa opieralo nízke riziko pri S3. Pri systémovom + bulk + reálne PII +
  týždne + trvalo nedostupné logy sa rizikový profil **VÝRAZNE SPRÍSŇUJE**.
- **Záver rizikovosti (REVIDOVANÝ):** pri potvrdení bulk-enumerability zoznamových endpointov sa
  riziko posúva na **STREDNÉ–VYSOKÉ** (anonym vie stiahnuť celé datasety reálnych klientov) a výnimka
  čl. 33/1 „unlikely to result in a risk" sa stáva **ťažko udržateľnou**. Ak by sa (nepravdepodobne)
  potvrdilo, že VŠETKY sú id-gated, ostáva nízke-stredne nízke. **Rozhodujúci vstup = bulk-vs-id-gated
  potvrdenie od Bezpecnost.**
- **BULK výsledky (Bezpecnost, 2026-06-07) — REKALIBRÁCIA NADOL:**
  - `matching`×3 = **ID-GATED** → nízke.
  - `odberatelia` = **ID-GATED** → nízke.
  - `manazer/sla` = bulk-capable, ale **0 dát** → reálne niet čo uniknúť.
  - `objednavky` = **BULK, ale len 3 záznamy a PSEUDONYMNÉ** — obsahuje klient_id (UUID FK) +
    druh/požiadavky/lokalita/cena; ŽIADNE priame meno/telefón (tie sú v `klienti`, ktorá NEBOLA
    anon-čítateľná). Re-id vyžaduje ĎALŠÍ leak → bariéra zachovaná. ⇒ nízky dopad napriek „bulk".
  - `logy` = **LEN POČTY** (GET `head:true`, count-only — vráti počty {klienti, naberove_listy,
    objednavky, nehnutelnosti}, ŽIADNY obsah/riadky/PII). ⇒ **NIE je to leak osobných údajov.**
- **Záver rizikovosti (FINÁLNY):** obávaný „bulk dump priamych identifikátorov" sa **NEPOTVRDIL na
  žiadnom endpointe**. Jediný bulk (`objednavky`) je pseudonymný + 3 záznamy + bariéra re-id zachovaná;
  zvyšok id-gated / bez dát / len počty. ⇒ riziko **HRANIČNÉ-NÍZKE**.

## 6. Rozhodnutie o oznámení (čl. 33 / 34) — FINÁLNE
> **Vývoj stanoviska (transparentne):** (1) len S3/S7 UUID-gated → neoznamovať; (2) systémový rozsah
> ~12 endpointov, obava z bulk → sprísnené na „pripraviť oznámenie"; (3) bulk + obsah logy overené →
> obávaný priamy-identifikátor bulk dump sa NEPOTVRDIL → finálna rekalibrácia NADOL. **Toto je finál.**
- **ÚOOÚ (čl. 33 ods. 1): NEOZNAMOVAŤ.** Odôvodnenie: žiadny endpoint nevystavil priame identifikátory
  hromadne. Jediný bulk (`objednavky`) = pseudonymný (UUID + dopyt/rozpočet, bez mena/telefónu) + 3
  záznamy + bariéra re-id zachovaná (klienti nebola anon-čítateľná); odberatelia + matching id-gated;
  manazer/sla bez dát; logy len počty. Žiadny dôkaz prístupu, nález interný. Riziko pre práva osôb je
  nízke → uplatňuje sa výnimka čl. 33/1 „unlikely to result in a risk". **Ratifikuje advokát pred go-live
  (poistka, nie povinnosť) — núdzový 72h režim sa NEAKTIVUJE.**
- **Dotknuté osoby (čl. 34): NEOZNAMOVAŤ** — vysoké riziko nesplnené.
- **Povinné aj tak:** zaznamenať do breach registra (čl. 33/5, tento dokument); Machovic→Vianema
  notifikácia (sekcia 8); zavrieť všetky zostávajúce anonymné routes (komplet hotfix); doplniť
  read-audit-log (čl. 5/2) na PII endpointy.

## 7. Prijaté / potrebné opatrenia
- [x] 🔴 **Zavretá S3 a S7 na prode** — hotfix `b5c7be9` na `main`, nasadený 2026-06-07 17:15:54 UTC. *(Bezpecnost/DevOps)*
- [x] **Post-deploy overené**: S3 = 401, S7 = 401 (bypass mŕtvy). *(Bezpecnost/DevOps)*
- [ ] Doplniť **audit log čítaní** na S3/S7 (budúca preukázateľnosť rozsahu). *(Bezpecnost)*
- [x] Zistiť a doplniť **okno expozície** (git history) — viď sekcia 4. *(Bezpecnost)*
- [x] 🔎 **Kontrola HOSTING access logov za okno expozície — VYKONANÁ, výsledok: dáta TRVALO
  NEDOSTUPNÉ.** Bezpecnost overila (2026-06-07): Vercel Hobby nemá log drains, runtime logy sú
  ephemeral, staré deploye superseded; Supabase prod je v cudzej org na Free/Nano s ~1-dňovou
  retenciou. ⇒ Neoprávnený prístup k S3/S7 počas okna **nemožno ani potvrdiť, ani vylúčiť**, a tento
  stav je **trvalý** (neexistuje žiadny zdroj dát). Zaznamenané ako fakt. *(Bezpecnost/DevOps)*
- [x] **Processor → Controller notifikácia** (Machovic → Vianema) — viď nižšie. *(Compliance)*
- [ ] Po nasadení migr. 108 preniesť tento záznam do tabuľky `breach_register`. *(Compliance)*
- [x] **Rozšírený hotfix S4 + matching** — `162948d`, 401 overené 2026-06-07 18:00:07 UTC. *(Bezpecnost)*
- [x] **Komplet hotfix VŠETKÝCH anonymných routes** — 4 hotfix commity, všetky curl-overené 401,
  zatvorené 17:15–18:23 UTC 2026-06-07. **0 anonymných klientskych-PII routes na prode.** *(Bezpecnost)*
- [x] ✅ **Podmienka „všetky anonymné P0 zavreté pred reálnymi klientmi" = SPLNENÁ.**

## 8. Notifikácia spracovateľ → prevádzkovateľ (čl. 33 ods. 2)
**Od:** Machovic s.r.o. (spracovateľ) · **Komu:** Vianema s.r.o. (prevádzkovateľ) · **Dátum:** 2026-06-07

Ako spracovateľ Vás bez zbytočného odkladu informujeme o bezpečnostnej zraniteľnosti zistenej
2026-06-07 v CRM systéme na doméne vianema.amgd.sk: dva endpointy (`/api/klient-udalosti`,
`/api/cron/volni-klienti`) boli dočasne prístupné bez autentifikácie a umožňovali anonymný prístup
k údajom klientov, resp. spúšťanie operácií. Zraniteľnosť bola zistená interným auditom, bez dôkazu
skutočného zneužitia. Riziko vyhodnocujeme ako nízke. Prijímame okamžité opatrenia (zatvorenie
endpointov, nasadenie opravy, doplnenie logovania prístupu). Odporúčanie k oznámeniu ÚOOÚ: na
základe súčasných faktov oznámenie nie je povinné; konečné rozhodnutie odporúčame potvrdiť právnym
zástupcom. O dokončení nápravných opatrení Vás budeme informovať.

## 9. Stav záznamu
- **2026-06-07 (S3/S7 časť):** Zavreté a overené (401). Log forenzika vykonaná — access dáta trvalo
  nedostupné → prístup nemožno potvrdiť ani vylúčiť (sekcia 7).
- **2026-06-07 (ROZŠÍRENIE — systémový rozsah, UZAVRETÉ):** Incident rozšírený na ~16 anonymných PII
  routes (koreňová príčina: hardening vrstva len na dev). **VŠETKY zatvorené 4 hotfix commitmi,
  curl-overené 401 (17:15–18:23 UTC).** Kompletné okná expozície v sekcii 4, bulk-vs-id-gated v sekcii 5.
  **0 anonymných klientskych-PII routes na prode.**
- **FINÁLNY compliance záver (2026-06-07):** žiadny endpoint nevystavil priame identifikátory hromadne
  (objednavky bulk ale pseudonymné/3 záznamy/bariéra re-id; ostatné id-gated/bez dát/len počty;
  najdlhšie okno ~64 dní, ale id-gated → dĺžka nepomáha bez ID) → riziko HRANIČNÉ-NÍZKE →
  **ÚOOÚ ani dotknutým osobám NEOZNAMOVAŤ** (výnimka čl. 33/1). Zaznamenané v registri;
  processor→controller notifikácia vykonaná. Núdzový 72h režim sa NEAKTIVUJE.
- **Zostáva (mimo compliance / nie urgentné):** (a) read-audit-log na PII endpointy (Bezpecnost,
  prevencia); (b) ratifikácia neoznámenia právnikom pred go-live (poistka, nie povinnosť);
  (c) po nasadení migr. 108 na vianemu preniesť záznam do tabuľky `breach_register`.

---

## 10. Rozšírenie — anonymne čitateľné views (dáta MAKLÉROV), 2026-06-08
> Iný vektor a iné dotknuté osoby než sekcie 1–9 (tam klienti; tu makléri) → samostatné posúdenie,
> ale tá istá udalosť/koreňová príčina (security vrstva nesprávne aplikovaná na prod) → vedené v tomto
> konsolidovanom zázname.

- **Nález (Bezpecnost, Supabase Advisor CRITICAL ×2):** SECURITY DEFINER views `v_deals` +
  `v_makler_stats` (migr. 043) mali `GRANT SELECT ... TO anon` → anonymne čitateľné cez verejný anon
  kľúč BEZ prihlásenia. Vektor = anon GRANT na views (nie chýbajúci requireUser) — trieda, ktorú RoPA
  discovery avizoval (RLS/anon politiky).
- **Dáta:** `v_deals` per obchod = obrat (predajná cena), provízia firmy, typ provízie, **meno makléra
  + pracovný e-mail makléra**. `v_makler_stats` per maklér = meno, e-mail, celkový obrat, priemerná
  provízia firmy, počet obchodov. **NIE klientske PII** (žiadne mená/telefóny predávajúcich).
- **Dotknuté osoby:** MAKLÉRI Vianemy (nie klienti) — malý známy interný okruh.
- **Kategórie OÚ:** meno + pracovný e-mail (nízka citlivosť, často poloverejné — na inzerátoch/webe)
  + výkonnostné agregáty per menovaný maklér (obrat / provízia FIRMY — obchodná produktivita, **NIE
  osobná mzda/príjem makléra**); žiadna osobitná kategória (čl. 9).
- **Okno expozície:** od migr. 043 = **2026-05-08** (commit `a9c9d77`) po DROP. Dev DROP-nuté
  (migr. 112); prod DROP schválil CEO (cez dashboard, 2026-06-08). ≈ **30 dní**. Views sú MŔTVE
  (appka ich nepoužíva). Bulk (vrátia všetky riadky), ale na exploitáciu treba anon kľúč + znalosť
  názvov views (nie plain URL).
- **Riziko:** **NÍZKE.** Malý interný okruh maklérov; kontaktné dáta poloverejné; výkonnostné agregáty
  (firemná provízia/obrat, nie osobná mzda); žiadny dôkaz prístupu; mŕtve views; nález interný.
- **Rozhodnutie o oznámení:**
  - **ÚOOÚ (čl. 33): NEOZNAMOVAŤ** — riziko pre práva maklérov nízke → výnimka čl. 33/1. Ratifikuje
    právnik (spolu s hlavným incidentom).
  - **Makléri (čl. 34): formálne netreba** (vysoké riziko nesplnené). ALE keďže sú to vlastní
    spolupracovníci, **odporúčam interne ich informovať** o krátkej expozícii ich výkonnostných/kontaktných
    údajov — dobrá prax (transparentnosť, dôvera), nie zákonná povinnosť.
  - Zaevidovať do breach registra.
- **Zostáva:** po prod DROP overiť, že `v_deals`/`v_makler_stats` už NIE sú anon-čitateľné (Supabase
  Advisor clean); zvážiť exhaustívny Advisor sweep na ďalšie anon GRANT/RLS úniky (Bezpecnost).
